let mongoose;_43e‍.x([["default",()=>_43e‍.o]]);_43e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const { Schema } = mongoose;

const TodoSchema = new Schema({
    title: String,
    checked: { type: Boolean, default: false },
    publishedDate: {
        type: Date,
        default: Date.now,
    },
});

const todoModel = mongoose.model('Todo', TodoSchema);
_43e‍.d(todoModel);
