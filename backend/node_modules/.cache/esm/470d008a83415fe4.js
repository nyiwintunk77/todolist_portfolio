let Koa,Router,mongoose,api,bodyParser;_834‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_834‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_834‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_834‍.w("./api",[["default",["api"],function(v){api=v}]]);_834‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);





require('dotenv').config();
const { PORT, MONGO_URL } = process.env;

mongoose
    .connect(MONGO_URL, { keepAlive: true, useNewUrlParser: true, useFindAndModify: false })
    .then(() => {
        console.log('Connected to MongoDB');
    })
    .catch((e) => {
        _834‍.g.console.log(e);
    });

const app = new Koa();
const router = new Router();

router.use('/api', api.routes());

app.use(bodyParser());
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    _834‍.g.console.log('Server Start!!at%d', port);
});
